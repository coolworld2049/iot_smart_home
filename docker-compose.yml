version: '3.9'

services:
  emqx1:
    image: emqx:5.1.6
    container_name: emqx1
    environment:
    - EMQX_NODE_NAME=emqx@node1.emqx.io
    - EMQX_CLUSTER__DISCOVERY_STRATEGY=static
    - EMQX_CLUSTER__STATIC__SEEDS=[emqx@node1.emqx.io,emqx@node2.emqx.io]
    healthcheck:
      test: ["CMD", "/opt/emqx/bin/emqx", "ctl", "status"]
      interval: 5s
      timeout: 25s
      retries: 5
    networks:
      default:
        aliases:
          - node1.emqx.io
    ports:
      - "1883:1883"
      - "8083:8083"
      - "8084:8084"
      - "8883:8883"
      - "18083:18083"
    volumes:
      - emqx1_data:/opt/emqx/data
  emqx2:
    image: emqx:5.1.6
    container_name: emqx2
    environment:
    - EMQX_NODE_NAME=emqx@node2.emqx.io
    - EMQX_CLUSTER__DISCOVERY_STRATEGY=static
    - EMQX_CLUSTER__STATIC__SEEDS=[emqx@node1.emqx.io,emqx@node2.emqx.io]
    healthcheck:
      test: ["CMD", "/opt/emqx/bin/emqx", "ctl", "status"]
      interval: 5s
      timeout: 25s
      retries: 5
    networks:
      default:
        aliases:
          - node2.emqx.io
    volumes:
      - emqx2_data:/opt/emqx/data
    depends_on:
      emqx1:
        condition: service_healthy
  gateway:
    build: .
    hostname: gateway
    command: python -m iot_smart_home.devices.gateway
    env_file:
      - .env.prod
    environment:
      IOT_SMART_HOME_MQTT_TOPIC: gateway
    restart: always
  sensor-climate:
    build: .
    hostname: sensor-climate
    command: python -m iot_smart_home.devices.sensors.climate
    env_file:
      - .env.prod
    environment:
      IOT_SMART_HOME_MQTT_TOPIC: sensors/climate
    restart: always
  sensor-lamp:
    build: .
    hostname: sensor-lamp
    command: python -m iot_smart_home.devices.sensors.lamp
    env_file:
      - .env.prod
    environment:
      IOT_SMART_HOME_MQTT_TOPIC: sensors/lamp
    restart: always
  sensor-motion:
    build: .
    hostname: sensor-motion
    command: python -m iot_smart_home.devices.sensors.motion
    env_file:
      - .env.prod
    environment:
      IOT_SMART_HOME_MQTT_TOPIC: sensors/motion
    restart: always
  homeassistant:
    container_name: homeassistant
    image: ghcr.io/home-assistant/home-assistant:stable
    restart: unless-stopped
    environment:
      - TZ=Europe/Moscow
    volumes:
      - ./homeassistant/config:/config
      - /etc/localtime:/etc/localtime:ro
    privileged: true
    ports:
      - "8123:8123"
    depends_on:
      - emqx2
volumes:
  emqx1_data:
  emqx2_data:
